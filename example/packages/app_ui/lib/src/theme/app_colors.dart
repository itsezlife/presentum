// ignore_for_file: public_member_api_docs

import 'package:flutter/material.dart';

class AppColors {
  static const lightIconDefault = Color(0xFFB3B3B3);

  static const lightIconHighlighted = Color(0xFF222222);

  static const lightBorderGradient = [
    Color(0xFF00B2FF),
    Color(0xFFA8E6FF),
    // Color(0xFFBB3FDD),
    Color(0xFF00B2FF),
  ];

  static const lightScrim = Color(0x4C000000);

  static const lightAvatarEmptyPlaceholderColor = Color(0xFFE5E7EB);

  static const lightAvatarPlaceholderColors = [
    Color(0xFF3B82F6), // Blue
    Color(0xFF10B981), // Emerald
    Color(0xFFF59E0B), // Amber
    Color(0xFFEF4444), // Red
    Color(0xFF8B5CF6), // Violet
    Color(0xFF06B6D4), // Cyan
    Color(0xFFF97316), // Orange
    Color(0xFF84CC16), // Lime
    Color(0xFFEC4899), // Pink
    Color(0xFF6366F1), // Indigo
  ];

  static const textHighlightedLight = Color(0xFF007AFF);

  static const lightHintColor = Color(0x99000000);

  static const darkIconDefault = Color(0xFF666666);

  static const darkIconHighlighted = Color(0xFFFFFFFF);

  static const darkBorderGradient = [
    Color(0xFF797579),
    Color(0xFFFFFCFF),
    Color(0xFF797579),
  ];

  static const darkScrim = Color(0x99000000);

  static const darkAvatarEmptyPlaceholderColor = Color(0xFF4B5563);

  static const darkAvatarPlaceholderColors = [
    Color(0xFF1E40AF), // Darker Blue
    Color(0xFF059669), // Darker Green
    Color(0xFFD97706), // Darker Amber
    Color(0xFFDC2626), // Darker Red
    Color(0xFF7C3AED), // Darker Purple
    Color(0xFF0891B2), // Darker Cyan
    Color(0xFFEA580C), // Darker Orange
    Color(0xFF65A30D), // Darker Lime
    Color(0xFFDB2777), // Darker Pink
    Color(0xFF4F46E5), // Darker Indigo
  ];

  static Color textHighlightedDark = Colors.lightBlue[400]!;

  static const Color darkHintColor = Colors.white60;

  static const Color transparent = Color(0x00000000);

  static const Color gainsboro = Color(0xFFDADCE0);

  static const Color scrim = Colors.black54;
}
